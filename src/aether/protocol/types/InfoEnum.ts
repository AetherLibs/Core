export enum Types {
    IDENTIFY_CLIENT,
    LAUNCH_CLIENT,
    READY_CLIENT,
    RESTART_CLIENT,
    KILL_CLIENT,
    RESUME_CLIENT,
    HEARTBEAT,
    HEARTBEAT_ACK,
    DEPLOY,
    SEND_STATS,
    REQUEST_STATS,
    LOAD_MODULE,
    BROADCAST_EVAL,
    ADMIN_ACTION,
    SETTINGS_SYNC,
    BLACKLIST_SYNC,
    CROSS_CLUSTER_QUOTE,
    UPDATE_SOLUTIONS,
    PLAYWRIGHT_REQUEST,
    SLASH_COMMAND,
    API_REQUEST,
    ALIAS_SYNC,
  }
  
  export enum States {
    CONNECTING,
    CONNECTED,
    ERROR,
    CUTE,
    CLOSING,
    CLOSED,
    RECONNECTING,
    IDLE,
  }
